server {
       listen 80;
       listen [::]:80;

       server_name sddl.crigamelab.org;

       location / {
               client_max_body_size 0;
               proxy_pass http://localhost:3040/;
       }
}